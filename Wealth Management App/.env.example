# Optional: only if you call Alpha Vantage directly from the frontend.
# Prefer routing all market data through the FastAPI backend (no keys in the browser).
VITE_API_BASE=
VITE_ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key_here
